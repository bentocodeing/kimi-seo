---
description: Kimi SEO suite — run any SEO command (audit, page, technical, content, schema, sitemap, geo, local, backlinks, ...) through the seo orchestrator skill
---

Invoke the `seo` skill with the arguments below, then follow its instructions
exactly. In particular, full audits must persist their artifacts to disk in
the current working directory — `{domain}-audit/` containing
`FULL-AUDIT-REPORT.md`, `ACTION-PLAN.md`, `audit-data.json`, `findings/`, and
`screenshots/` — not just summarize findings in chat.

ARGUMENTS: $ARGUMENTS
